{
  "name": "psrembalagens-website",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./custom.css -o ./dist/style.css --minify",
    "dev": "tailwindcss -i ./custom.css -o ./dist/style.css --watch",
    "start": "echo 'Site pronto para uso'"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "fs": "^0.0.1-security"
  }
}
